apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sprcloudaliorder-app
  namespace: default
  labels:
    team: default
spec:
  selector:
    matchLabels:
      app: sprcloudaliorder
  endpoints:
    - port: web
      path: /prometheusMetrics
