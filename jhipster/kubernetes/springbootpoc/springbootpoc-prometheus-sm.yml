apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: springbootpoc-app
  namespace: default
  labels:
    team: default
spec:
  selector:
    matchLabels:
      app: springbootpoc
  endpoints:
    - port: web
      path: /prometheusMetrics
