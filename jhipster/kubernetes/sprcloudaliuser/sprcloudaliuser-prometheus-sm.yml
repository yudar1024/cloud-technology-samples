apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sprcloudaliuser-app
  namespace: default
  labels:
    team: default
spec:
  selector:
    matchLabels:
      app: sprcloudaliuser
  endpoints:
    - port: web
      path: /prometheusMetrics
